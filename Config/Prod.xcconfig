// SPEC §10 환경 분리 — Release(Prod) 빌드 구성. 공개 앱 정체성만 담고 커밋한다.
#include "Version.xcconfig"

APP_PHASE = production
APP_BUNDLE_ID = com.ninepiece.app.mac.plateshelf
APP_DISPLAY_NAME = plateshelf
APP_ICON_NAME = AppIcon
API_BASE_URL =
SEO_BASE_URL =
SUPPORT_EMAIL =
