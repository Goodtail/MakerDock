// Single source of truth for both production and development builds.
// Public versions begin at 0.1.0; routine development edits keep this version.
// Increment only the internal build number for local rebuilds and small fixes.
// Change MARKETING_VERSION when the user requests a new public version/release.
MARKETING_VERSION = 0.1.1
CURRENT_PROJECT_VERSION = 15
