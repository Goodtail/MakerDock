// 버전 단일 출처(커밋 대상). 릴리스마다 여기서 올린다.
// MARKETING_VERSION      = 사용자에게 보이는 버전 (CFBundleShortVersionString)
// CURRENT_PROJECT_VERSION = 빌드 번호 (CFBundleVersion) — 업로드마다 증가해야 한다
MARKETING_VERSION = 1.5.1
CURRENT_PROJECT_VERSION = 10
