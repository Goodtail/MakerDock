# MakerDock 0.1.1 — local build 16

- Keep the MakerWorld page with downloads initiated in the embedded browser, including Studio links. Existing unrelated page links are never guessed from filenames or internal IDs.
- Calculate missing queue estimates automatically with the selected printer. Save the result for every view of the model; process calculations one at a time and expose failed jobs for retry.
- Mark a queue item as printing, edit its actual start time, see remaining time, and record completion. Keep active work first and account for elapsed time in the plan. Printer status is still entered manually.
- Fit the sidebar, model library, and detail panel to the window. Wrap controls at smaller widths.
- Keep existing library records and archive files. Public version stays 0.1.1; internal build increases to 16.

This is a local update, not a replacement of the published build 15 release asset.
